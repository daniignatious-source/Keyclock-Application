- name: Deploy Nginx web server for static page
  docker_container:
    name: nginx-web
    image: nginx:latest
    state: started
    published_ports:
      - "80:80"
    volumes:
      - /path/to/your/static_website:/usr/share/nginx/html
